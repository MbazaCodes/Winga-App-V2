name: winga_mobile
description: Winga Mobile App — Tanzania Shopping Guide
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  google_maps_flutter: ^2.5.0
  firebase_messaging: ^14.0.0
  mpesa_flutter_plugin: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
